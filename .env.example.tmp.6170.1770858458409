# Patreon OAuth2 credentials
# Get these from https://www.patreon.com/portal/registration/register-clients
PATREON_CLIENT_ID=your_client_id_here
PATREON_CLIENT_SECRET=your_client_secret_here
PATREON_REDIRECT_URI=https://skelspanels.com/api/auth/callback

# JWT secret for signing auth cookies
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_random_secret_here_at_least_32_chars

# Your Patreon page URL (used in upgrade links)
PATREON_PAGE_URL=https://www.patreon.com/skeldoor
